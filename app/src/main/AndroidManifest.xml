<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asad.volontaireos">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.android.maps.v2.API_KEY"
            android:value="AIzaSyDAFoS5Hs6UwNVus50WI__PW-oYvQV37YY" />


        <activity android:name=".Sign_Up_Activity" />
        <activity android:name=".Login_Activity" />
        <activity android:name=".Main_Page" />
        <activity android:name=".P2_SignUp_LogIn" />

        <activity android:name=".A0_Welcome_Page">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

        <activity android:name=".A1_Main" />
        <activity android:name=".A2_Volunteer_Login" />
        <activity android:name=".A3_Maps" />
        <activity android:name=".A4_Requester_Login" />
        <activity android:name=".A5_Volunteer_Map_Activity" />
        <activity android:name=".A5_VM" />
        <activity android:name=".A55_VVM" />

        <activity android:name=".Choice_Page" />
        <activity android:name=".L_G_S" />



        <!--activity
            android:name=".Maps_Activity"
            android:label="@string/title_activity_maps" /-->

        <!--service
            android:name=".TrackerService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".TrackerActivity"></activity-->
    </application>

</manifest>