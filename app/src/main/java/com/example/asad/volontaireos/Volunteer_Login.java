package com.example.asad.volontaireos;

import android.support.v7.app.AppCompatActivity;

public class Volunteer_Login extends AppCompatActivity {
   /* private EditText mEmail , mPassword;
    private Button mLoginIn , mRegister ;

    private FirebaseAuth mAuth;
    private FirebaseAuth.AuthStateListener firebaseAuthlistner;
    @Override
    protected void onCreate(Bundle savedInstanceState){
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_volunteer_login);

        mAuth = FirebaseAuth.getInstance();
/*
        firebaseAuthlistner = new FirebaseAuth.AuthStateListener() {
            @Override
            public void onAuthStateChanged(@NonNull FirebaseAuth firebaseAuth) {
                FirebaseUser user = FirebaseAuth.getInstance().getCurrentUser();
                if(user != null) {
                    Intent b = new Intent(Volunteer_Login.this);
                            startActivity(b);
                            finish();
                            return;
                }
                }
            };


        mEmail = (EditText) findViewById(R.id.email);
        mPassword = (EditText) findViewById(R.id.pswd);

        mLoginIn = (Button) findViewById(R.id.login);
        mRegister =(Button) findViewById(R.id.Register);


    }*/

}
